apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - externalsecrets.yaml
  - postgres.yaml
  - redis.yaml
  - app.yaml
  - volsync.yaml
  # Temporary restore drill resources. Remove these entries after the test
  # so Argo CD prunes the throwaway restore objects.
  # TODO
  - restore.yaml
  - restore-test.yaml
