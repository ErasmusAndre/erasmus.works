apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Core
  - namespace.yaml
  - externalsecret.yaml
  - application.yaml

  # Networking
  - httproutes.yaml

  # Metrics
  - backup-alerts.yaml
  - kube-state-metrics-volsync-rbac.yaml
  - victorialogs-datasource.yaml
  - dashboards

  # SSO
  - authentik-oidc-externalsecret.yaml
  - securitypolicy.yaml
