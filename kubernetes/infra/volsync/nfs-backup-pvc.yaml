apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: volsync-repository
  namespace: apps
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: volsync-backups
