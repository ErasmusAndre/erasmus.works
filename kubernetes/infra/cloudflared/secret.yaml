apiVersion: v1
kind: Secret
metadata:
  name: cloudflared
  namespace: cloudflare-system
type: Opaque
stringData:
  # TODO: Replace this placeholder by sourcing the tunnel token from Bitwarden.
  tunnel-token: REPLACE_WITH_CLOUDFLARE_TUNNEL_TOKEN
